import * as types from '../_actions/AuthFormActions.jsx';

const initialState = {

}

export default function authFormReducers(state = initialState, action) {
    switch (action.type) {
        case types.AUTHORIZE_USER:
            return 
    }
}